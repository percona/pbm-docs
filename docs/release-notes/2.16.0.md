# Percona Backup for MongoDB 2.16.0 ({{date.2_16_0}})

[Installation](../installation.md){.md-button}

Percona Backup for MongoDB (PBM) creates consistent backups of MongoDB replica sets and sharded clusters with low impact. PBM supports restores to a specific point in time.

Version 2.16.0 adds backup retention policies, backup timing details, and experimental parallel uploads to Google Cloud Storage (GCS).

## Release highlights

### Backup lifecycle management

PBM supports backup retention policies based on the grandfather-father-son (GFS) model. Configure retention periods in the `lifecycle` section, then apply the policy with `pbm cleanup --lifecycle`.

The policy combines three retention periods:

| **Option** | **Retention behavior** |
| --- | --- |
| `dailyRetention` | Keeps all eligible backups within the specified number of days. |
| `weeklyRetention` | Selects one backup per backup type from each weekly interval within the retention period. |
| `monthlyRetention` | Selects one backup per backup type from each monthly interval within the retention period. |

Weekly and monthly retention support two strategies:

- The `rolling` strategy uses intervals of seven days and 30 days, measured backward from the cleanup evaluation time.

- The `calendar` strategy uses calendar weeks and months. PBM selects the backup closest to the configured weekday or day of the month within each interval.

PBM selects weekly and monthly backups separately for logical, physical, incremental, and external backup types. Selective logical backups are excluded from lifecycle cleanup.

The policy includes these safeguards:

- The `minKeep` option aborts cleanup if fewer than the specified number of successful restore points would remain. The default is `1`.

- The `purgeFailed` option controls deletion of failed and canceled backups. The default is `false`.

- PBM preserves an entire incremental backup chain when any member must remain.

- PBM protects base backups required for active point-in-time recovery (PITR). Lifecycle cleanup leaves PITR oplog slices in place.

!!! note "Preview the cleanup policy"

    Configure the retention periods and set `lifecycle.enabled` to `true`. Run `pbm cleanup --lifecycle --dry-run` to review the proposed changes without deleting backups.

    The policy takes effect when you run `pbm cleanup --lifecycle`. Use an external scheduler for recurring cleanup.

For a deep dive into this topic, see the [documentation]()

### Parallel uploads to Google Cloud Storage

The native GCS client supports parallel uploads of backup data. To enable parallel uploads, set these options:

- Set `storage.gcs.clientType` to `grpc`.

- Set `storage.gcs.parallelUploadConcurrency` to a value greater than `1`.

The GCS client uploads parts concurrently, then composes them into the final backup object. Performance depends on the backup size, available bandwidth, and host resources.

!!! warning "Experimental feature"

    Parallel uploads use an experimental feature of the Google Cloud Storage Go client. The upstream project does not recommend this feature for production use.

    The credentials require `storage.objects.delete` permission to remove temporary objects. Failed or interrupted uploads can leave these objects behind. Configure a bucket lifecycle rule for the `gcs-go-sdk-pu-tmp` prefix to remove leftover objects. Choose an age threshold that allows uploads to finish.

    See the upstream [parallel upload documentation :octicons-link-external-16:](https://pkg.go.dev/cloud.google.com/go/storage@v1.62.1#hdr-Parallel_Uploads) for cleanup requirements and storage costs.

For more insights into this feature, see the [documentation](https://pbm-docs-preview-pr-399.onrender.com/details/gcs.html#parallel-uploads-to-gcs).

### Extended platform support

PBM 2.16.0 adds packages for these platforms:

- Debian 13 (Trixie) on AMD64 and ARM64.

- Red Hat Enterprise Linux 10.

Red Hat packages use `systemd` to manage `pbm-agent`. The packages no longer include SysV init scripts.

## Upgrade considerations

### S3 debug logging

PBM validates `storage.s3.debugLogLevels` for Amazon Simple Storage Service (Amazon S3) and compatible storage. Unsupported values cause storage initialization to fail with an error.

If you use this option, review the supported values before upgrading:

`Signing`, `Retries`, `Request`, `RequestWithBody`, `Response`, `ResponseWithBody`, and `DeprecatedUsage`.


## Changelog

### New features

- [PBM-1699](https://perconadev.atlassian.net/browse/PBM-1699): Added backup start time, finish time, and duration to agent logs and `pbm describe-backup` output. The `pbm list` and `pbm status` output also includes duration.

- [PBM-1731](https://perconadev.atlassian.net/browse/PBM-1731): Added GFS backup retention policies and the `pbm cleanup --lifecycle` command to apply them.

- [PBM-1736](https://perconadev.atlassian.net/browse/PBM-1736): Added packages for Debian 13 (Trixie) on AMD64.

- [PBM-1770](https://perconadev.atlassian.net/browse/PBM-1770): Fixed an issue that could trigger a MongoDB fatal assertion during logical restores with namespace remapping on Percona Server for MongoDB 8.3.

- [PBM-1771](https://perconadev.atlassian.net/browse/PBM-1771): Fixed an archive-processing error that prevented logical restores of time-series collections on Percona Server for MongoDB 8.3.

- [PBM-1772](https://perconadev.atlassian.net/browse/PBM-1772): Fixed an `ObjectIsBusy` error during oplog replay in logical PITR restores on Percona Server for MongoDB 8.3.

- [PBM-1786](https://perconadev.atlassian.net/browse/PBM-1786): Added packages for Debian 13 (Trixie) on ARM64.

- [PBM-1806](https://perconadev.atlassian.net/browse/PBM-1806): Added packages for Red Hat Enterprise Linux 10. Red Hat packages use `systemd` and no longer include SysV init scripts.

### Improvements

- [PBM-1749](https://perconadev.atlassian.net/browse/PBM-1749): Added filesystem storage and storage configuration support to PBM-x.

- [PBM-1750](https://perconadev.atlassian.net/browse/PBM-1750): Added the initial control-agent and worker-agent components for PBM-x.

- [PBM-1776](https://perconadev.atlassian.net/browse/PBM-1776): Added experimental parallel uploads through the native GCS client's `grpc` client type.

- [PBM-1804](https://perconadev.atlassian.net/browse/PBM-1804): Updated version checks to recognize MongoDB and Percona Server for MongoDB 8.3. PBM no longer reports an unsupported minor version warning for these releases.

### Fixed bugs

- [PBM-1675](https://perconadev.atlassian.net/browse/PBM-1675): Corrected backup compatibility checks for sharded clusters. PBM also rejects connections through `mongos` with a clear error.

- [PBM-1689](https://perconadev.atlassian.net/browse/PBM-1689): Fixed an issue that left restores waiting indefinitely after backup chunk downloads failed. PBM reports the download error and stops the restore.

- [PBM-1700](https://perconadev.atlassian.net/browse/PBM-1700): Limited concurrent `collStats` queries during logical backup preparation to reduce the load on MongoDB.

- [PBM-1712](https://perconadev.atlassian.net/browse/PBM-1712): Fixed an issue that could prevent `pbm-agent` from completing cleanup after a physical restore.

- [PBM-1720](https://perconadev.atlassian.net/browse/PBM-1720): Added validation for `storage.s3.debugLogLevels`. PBM reports an error when an unsupported value prevents storage initialization.

- [PBM-1734](https://perconadev.atlassian.net/browse/PBM-1734): Fixed an issue that mixed S3 debug messages with `pbm status` and `pbm describe-backup` output.

- [PBM-1766](https://perconadev.atlassian.net/browse/PBM-1766): Fixed an error that prevented `pbm describe-backup` from displaying information about a failed logical backup.

- [PBM-1799](https://perconadev.atlassian.net/browse/PBM-1799): Fixed logical restores from GCS when backup files were split and `storage.gcs.prefix` was configured.

- [PBM-1824](https://perconadev.atlassian.net/browse/PBM-1824): Fixed a failure when PBM calculated collection sizes before a logical backup.
